# The published CLI defaults to the public GrabCode server, so most users
# never need this file. Only set GRABCODE_API_URL if you want to point at a
# different server (e.g. your own local dev instance):
# GRABCODE_API_URL=http://localhost:4000

# Optional: default password, used if -p isn't passed on the command line
# GRABCODE_PASSWORD=
